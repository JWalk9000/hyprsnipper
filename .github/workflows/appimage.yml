name: Build and Release AppImage

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  appimage-x86_64:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y wget file patchelf

      - name: Build AppImage (x86_64)
        env:
          ARCH: x86_64
          VERSION: ${{ github.ref_name }}
          APPIMAGE_EXTRACT_AND_RUN: 1
        run: |
          chmod +x packaging/appimage/build-appimage.sh
          packaging/appimage/build-appimage.sh
          # The build script creates HyprSnipper-${VERSION}-${ARCH}.AppImage
          # Create a symlink for consistent naming
          ln -sf HyprSnipper-${{ github.ref_name }}-x86_64.AppImage HyprSnipper-x86_64.AppImage

      - name: Compute checksum
        run: |
          sha256sum HyprSnipper-${{ github.ref_name }}-x86_64.AppImage | tee HyprSnipper-${{ github.ref_name }}-x86_64.AppImage.sha256

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: appimage-${{ github.ref_name }}-x86_64
          path: |
            HyprSnipper-${{ github.ref_name }}-x86_64.AppImage
            HyprSnipper-${{ github.ref_name }}-x86_64.AppImage.sha256

      - name: Upload to GitHub Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: |
            HyprSnipper-${{ github.ref_name }}-x86_64.AppImage
            HyprSnipper-${{ github.ref_name }}-x86_64.AppImage.sha256